# DTech AI

The production Worker calls Cloudflare Workers AI. The model can be changed in `applications/api/src/index.ts` as the model catalog evolves. AI output should be reviewed before automated publication until governance rules are finalized.
