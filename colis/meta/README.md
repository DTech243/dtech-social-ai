# Meta integration

The Worker contains the OAuth start/callback and webhook verification endpoints. Before real publishing, configure the Meta Developer App, production redirect URI, required products/permissions, and complete any App Review requirements Meta imposes for your chosen permissions.

Never put the Meta App Secret or access tokens in GitHub. Store them as Worker secrets.
