# PostgreSQL

Cloudflare Hyperdrive can connect Workers to an existing PostgreSQL database. Choose a managed provider such as Neon, Supabase, AWS RDS or another PostgreSQL provider. Run `colis/db/schema.sql` once against the database, then create the Hyperdrive configuration.
